# Відео-екосистема: один ролик → усі мережі

Мета: щоденний ролик, який ми вже генеруємо для TikTok, автоматично
розходиться по **TikTok · Instagram Reels · Facebook · Threads · YouTube
Shorts**, а коментарі з усіх відео-майданчиків падають в окремий Telegram-топік.

Статус: **Ф1 готова**, далі Ф2.
Дата: 2026-07-21.

Пов'язане: [TIKTOK_CONTENT_PLAN.md](TIKTOK_CONTENT_PLAN.md) — базовий пайплайн,
який ця робота розширює.

---

## 1. Що вже є

Пайплайн живий і працює щодня:

```
04:00  pick_product_for_today() → 9:16 фото → клip → ffmpeg-монтаж
18:00  publish_pick() → TikTok → видалення файлів → звіт у Telegram
```

| Компонент | Файл | Стан |
|---|---|---|
| Ротація без повторів | `services/tiktok_rotation.py` | ✅ |
| Генерація відео | `services/tiktok_video.py`, `tiktok_montage.py` | ✅ |
| Публікація в TikTok | `services/tiktok_publish.py` | ✅ |
| Планувальник | `management/commands/tiktok_scheduler.py` | ✅ |
| **IG Reels** | `services/meta.py:92` `publish_instagram_reel()` | ✅ написано, **не підключено до щоденного ролика** |
| **FB video** | `services/meta.py:122` `publish_facebook_page_video()` | ✅ написано, **не підключено** |
| Коментарі IG/FB | `services/meta_comments.py` (вебхук) | ✅ |
| Топіки Telegram | `services/comment_notify.py:_staff_target()` | ✅ механізм є |

Тобто дві з чотирьох нових площадок — це **не інтеграція, а розгалуження
в кінці пайплайну**.

---

## 2. Чому не можна просто дописати виклики в `publish_pick`

Три речі ламаються одразу.

### 2.1 Файл помирає зарано

`publish_pick` видаляє монтаж **одразу** після `PUBLISH_COMPLETE` від TikTok
(`tiktok_publish.py`, `_delete(montage_path)`). Meta і Threads тягнуть файл по
URL **асинхронно**: контейнер обробляється вже після того, як API повернув id.
Видалити раніше — вони отримають 404, і пост **тихо** не з'явиться.

YouTube робить гірше: він не тягне по URL взагалі, йому потрібен локальний файл
для resumable upload.

**Рішення:** файли не видаляються в кінці публікації. Окремий прохід чистить
монтажі **старші за 24 години**. Один зайвий mp4 на добу коштує копійки, а весь
клас гонок зникає.

### 2.2 Частковий збій ламає модель статусів

Зараз `publish_pick` падає на першій помилці і ставить піку `failed`. При
п'яти площадках TikTok може пройти, а Threads впасти. Тоді:

- ретрай перепостить TikTok → **дубль у стрічці**;
- або ми не ретраїмо → Threads втрачено назавжди;
- `pick.status` не описує реальність, бо реальність тепер — вектор із 5 значень.

**Рішення:** статус на кожну площадку окремо. Нова модель `VideoDelivery`
з `unique_together (pick, platform)`. Ідемпотентність стає наслідком схеми,
а не дисципліни.

### 2.3 Один капшен на всіх не налазить

| Площадка | Ліміт | Особливість |
|---|---|---|
| TikTok | 2200 | лінки не клікабельні |
| IG Reels | 2200 | до 30 хештегів |
| Facebook | ~без ліміту | **лінки клікабельні** → даємо пряме посилання на товар |
| Threads | ~500 | інша логіка хештегів |
| YouTube | title 100 + description 5000 | **потрібен заголовок**, якого немає в інших |

---

## 3. Архітектура

### 3.1 Нова модель `VideoDelivery`

```
pick          FK → TikTokDailyPick
platform      tiktok | instagram | facebook | threads | youtube
status        pending | publishing | published | published_private | failed | skipped
external_id   (індекс — по ньому шукаємо коментарі)
external_url
error, attempts, published_at
```

Свідомі рішення:

- **Не перевикористовуємо `SocialDelivery`.** Вона належить `SocialPost` —
  моделі *ручних* постів з адмінки. Зшивання зробило б `post_id` нулабельним
  і зламало б інваріант «доставка завжди належить посту». Ручний пайплайн
  не чіпаємо взагалі — це головна вимога.
- `skipped` ≠ `failed`. Вимкнена або ненастроєна площадка — не помилка.
  Інакше звіт щодня кричатиме про YouTube, який ми свідомо не вмикали.
- `published_private` — назва вже вживається в `SocialDelivery`. Знадобиться
  і для TikTok до аудиту, і для YouTube до compliance-аудиту.

### 3.2 Реєстр адаптерів

Кожна площадка — об'єкт з однаковим контрактом:

```
name, is_configured(), is_enabled(settings), needs_local_file,
caption(pick, script), publish(pick, script, video_url, local_path) -> dict
```

`publish_pick` перестає знати про TikTok. Він проходить реєстр, для кожної
площадки бере/створює `VideoDelivery`, пропускає вже опубліковані, викликає
адаптер, фіксує результат. Одна помилка не зупиняє решту.

### 3.3 Ротація: успіх = перша успішна площадка

Товар вибуває з циклу, якщо опублікувалась **хоч одна** площадка — не всі.

Інакше при збої Threads товар повернеться в пул і вийде вдруге в TikTok, де
вже був. Це неочевидно, тому в коді має бути коментар: наступний читач
захоче «виправити» на `all()`.

### 3.4 Рознесення в часі — закласти, не вмикати

Поле `publish_offset_minutes` на площадку, поки всюди `0`.

Ідентичне відео на п'яти площадках секунда-в-секунду читається як бот.
Але переробляти планувальник на живому автопостингу дорого, а закласти точку
розширення зараз — дешево.

---

## 4. Telegram: окремий топік для відео-мереж

Механізм є: `_staff_target()` віддає `(chat_id, thread_id)`, а
`notify_staff_*` кладуть `message_thread_id` у payload. Форум-топік — той
самий чат, інший thread.

Додається `SocialSettings.video_comments_thread_id`.

### Маршрутизація — за постом, не за площадкою

Здавалося б, достатньо «Instagram → відео-топік». Ні: Instagram живе в обох
світах — під Reels і під фото-каруселями товарів.

Тому: вхідний коментар несе `media_id`. Якщо він збігається з
`VideoDelivery.external_id` — відео-топік. Ні — наявний топік коментарів.
Нічого не знайшли — старий топік, щоб нічого не загубилось.

Критерій надійний і сам собою вирішує Threads та YouTube, чиї коментарі
за визначенням відео.

---

## 5. Фази

### Ф1 · Фундамент — нічого не змінюється назовні ✅

- ✅ модель `VideoDelivery` + міграція `0013`
- ✅ `TikTokDailyPick.Status` += `partial`, поле `montage_path`
- ✅ `services/video_networks.py` — реєстр адаптерів, TikTok перший і поки єдиний
- ✅ `publish_pick` розіслано по мережах, збій однієї не валить решту
- ✅ `cleanup_old_media()` — прохід по віку (24 год), викликається о 04:00
  і руками через `tiktok_daily --cleanup`
- ✅ адмінка: інлайн доставок на піку, колонка «Мережі», окремий список
  `VideoDelivery`
- ✅ **виправлено ризик №5** — `tiktok_auto_enabled` і решта відео-полів
  тепер видимі в Social settings (раніше їх не було в жодному fieldset)
- ✅ 15 нових тестів (`test_video_networks.py`), 344 зелені

Дві поведінки, які змінились свідомо:

1. **Файли переживають публікацію** — видаляються окремим проходом наступної
   доби, бо Meta/Threads тягнуть асинхронно, а YouTube читає з диска.
2. **`PARTIAL` не блокує повтор.** Він означає, що якась мережа ще не
   відпрацювала — саме для цього повтор і потрібен. Від дублів захищає
   унікальність `(pick, platform)`, а не статус піка. При цьому `PARTIAL`
   усе одно виводить товар з ротації: відео вже бачила аудиторія.

**Готовність:** щоденний TikTok працює рівно як раніше.

### Ф2 · Telegram відео-топік ✅ (код готовий, чекає на thread_id)

- ✅ поле `video_comments_thread_id` (міграція `0014`) + видиме в адмінці
- ✅ `notify_staff_text(..., video=True)` і `notify_staff_comment(..., video=True)`
- ✅ звіти щоденного ролика переїхали туди
- ✅ **ізоляція**: `video_comments_thread_id` не може дорівнювати
  `orders message_thread_id` — інакше звіти потрапили б в AI обробки замовлень
- ✅ порожній `video_comments_thread_id` = фолбек у звичайний топік коментарів,
  щоб звіт не зник у тиші

⏳ **Лишилось від тебе:** створити топік і дати мені його `thread_id` (крок №3
у розділі 9).

### Ф3 · Instagram Reels + Facebook Reels ✅ (код готовий, чекає на вмикання)

- ✅ `video_caption.py` — одна структура фактів, рендер під кожну мережу
- ✅ адаптери `InstagramReelsAdapter`, `FacebookReelsAdapter`
- ✅ перемикачі `video_instagram_enabled` / `video_facebook_enabled` (+ Threads,
  YouTube наперед), міграція `0015`
- ✅ маршрутизація коментарів за постом, поле `post_id` (міграція `0016`)
- ✅ 20 нових тестів, 366 зелених

**Facebook: перейшли на справжні Reels.** Дослідження змінило рішення:
`/{page}/videos` з `file_url` працює, але **не задокументований** і дає
звичайну відео-роздачу. Тепер `/{page}/video_reels` у три фази.

Ключове відкриття: **pull з URL там є**, просто `file_url` передається
**HTTP-заголовком** на фазі upload, а не параметром. Тобто бінарний аплоад
не потрібен — інфраструктура не змінюється.

⚠️ Meta тягне файл як `facebookexternalhit/1.1`. Перевірив: наш nginx
User-Agent не фільтрує. Якщо колись з'явиться захист від ботів — це зламає
Reels з дуже неочевидною помилкою.

**Facebook-пастка, яку вирішили:** Reels API повертає `video_id`, а вебхук
коментарів приносить `post_id` — різні рядки. Без зіставлення коментарі
з FB ніколи б не розпізнались як відео. Тому `post_id` резолвиться одразу
після публікації і зберігається окремим полем; `is_video_post()` звіряє обидва.

**Капшени — правило ціни застосовано за видимістю, а не однаково:**

| Мережа | Що видно одразу | Рішення |
|---|---|---|
| TikTok / IG / FB | перші 1–2 рядки | розміри нижче, під «ще» |
| **Threads** | **весь пост** | **ціну не пишемо взагалі** |
| YouTube title | найпомітніший текст | лише питання-гачок |

Threads тут не «коротший TikTok»: там немає куди сховати ціну, тож формат
«вгадай» вимагає прибрати її повністю. Хто хоче число — дивиться або питає.

⏳ **Лишилось від тебе:** увімкнути Instagram і Facebook у Social settings
(і перевірити, що Meta page token живий).

### Ф4 · Threads ✅ (код готовий, чекає на OAuth)

- ✅ `ThreadsToken` (міграція `0017`) — 60 днів, **без** refresh_token
- ✅ `threads_auth.py` — OAuth на `threads.net`, обмін на long-lived токен
- ✅ `threads.py` — контейнер → полінг → `threads_publish`
- ✅ `ThreadsAdapter` у реєстрі, вимкнений за замовчуванням
- ✅ роути `/api/threads/authorize/` та `/api/threads/callback/` (staff-only)
- ✅ адмінка `ThreadsToken` з кнопкою авторизації
- ✅ 18 тестів, 384 зелені

**Ревʼю не потрібне** — для власного акаунта пости одразу публічні.

**Моє припущення про хештеги підтвердилось документацією** — і виявилось
глибшим, ніж я думав. Дослівно з докумeнтації Meta:

> Only one topic tag is allowed per post

Але головне інше: є **окремий параметр `topic_tag`**, а inline `#` Meta прямо
називає застарілим («kept for backwards compatibility»). Тому в тексті поста
хештегів немає взагалі — тег іде окремим полем. Обираємо найспецифічніший
(категорію), бо topic tag — це як спільнота знаходить пост, а не набір
ключових слів.

**Пастка з лімітом:** документація каже 500 символів, але URL та емодзі
рахує в байтах, і не уточнює, як бути з кирилицею (2 байти на символ).
Обрізаємо по обох межах одночасно — нам це нічого не коштує, бо текст
короткий, зате виключає мовчазний обрив на півслові.

**24-годинне вікно:** токен не можна оновити раніше ніж через добу після
видачі. Наївне «рефреш щоnight о 03:00» впало б у перший же день — тому
`old_enough_to_refresh` перевіряється до виклику, і в адмінці це видно
текстом, а не помилкою.

⏳ **Лишилось від тебе:** кроки 5-6 у розділі 9 — увімкнути Threads use case
у Meta-застосунку та натиснути «Дозволити» в OAuth.

### Ф5 · Коментарі з Threads ✅ (код готовий, чекає на підписку)

**Вебхук, не полінг.** Meta має окремий webhook-продукт для Threads:
topic `moderate`, field `replies`. Payload одразу несе `username`, `text`,
`permalink`, `root_post` — нічого дотягувати не треба.

Полінг був би гіршим: ні `/replies`, ні `/conversation` **не приймають
`since`**, тож довелося б перечитувати всю гілку щоцикл.

- ✅ `services/threads_comments.py` — розбір payload, дедуп, маршрутизація
- ✅ `/api/threads/webhook/` — handshake + перевірка `X-Hub-Signature-256`
- ✅ підпис звіряється з **Threads**-секретом (не Meta — ключі різні)
- ✅ `THREADS_WEBHOOK_VERIFY_TOKEN` згенеровано, вже на проді і в `ops/`
- ✅ 18 тестів, 413 зелених

**Пастка, на яку легко наступити:** `GET /me/replies` виглядає як очевидний
вибір, але повертає відповіді, які **написали ми**, а не отримали. Мовчки
віддає правдоподібні дані — і ти довго не розумієш, чому нічого немає.

**Дедуп у два шари.** Meta не гарантує exactly-once. Кеш у пам'яті ловить
звичайний випадок, але вмирає при редеплої — тому друга перевірка йде в базу
по `SocialCommentReply`.

**Свої відповіді відсіюються.** У webhook-payload немає
`is_reply_owned_by_me` (він є лише в REST), тож звіряємо `owner_id` і
`username` з нашими. Інакше відповідь клієнту прилітала б назад у топік як
нове питання.

⏳ **Лишилось:** задеплоїти, потім я підпишу вебхук у порталі
(Use Cases → Threads → Webhooks → topic `moderate`, field `replies`).

### Ф6 · YouTube Shorts ✅ (код готовий, дві гіпотези спростовано)

Дві речі з дослідження **не підтвердились на практиці** — обидві виявились
під час першого живого завантаження, і обидві варті запису.

**1. Замка «назавжди приватне» немає.** Дослідження стверджувало, що до
проходження compliance audit API мовчки ставить `private`. Реально відео
вийшло `public` з першого разу, на свіжому проєкті `mrcarpet-video`,
створеному 2026-07-21 — тобто після 28.07.2020, коли обмеження нібито діє.

Отже **compliance audit не потрібен**. Це знімає з плану найдовшу
залежність: тижні очікування перетворились на нуль.

**2. Вертикальності недостатньо для Shorts.** Файл 1080×1920, 13.5 с, h264 —
рівно те, під що YouTube класифікує Shorts. Він усе одно опублікував його як
звичайне відео. Тому в заголовок додано `#Shorts` — історичний явний сигнал,
який документація нібито скасувала, а практика показала, що ні.

Ще одна дрібниця, на якій легко обманутись: `/shorts/<id>` повертає **200
навіть для звичайного відео** — YouTube просто редіректить на `/watch`.
Перевіряти класифікацію за URL марно.

**Що підтвердилось:**

- ✅ `YouTubeToken`, OAuth, resumable upload (14.5 МБ пройшли), адаптер
- ✅ канал звірено через `snippet.channelId` у відповіді — **mr.Carpet**,
  `UCYssNNgxKFKEPSXRo8SDoLg`, без розширення скоупа
- ✅ `containsSyntheticMedia` прийнято, 400 не було
- ✅ 19 тестів

**Пастка, яку варто пам'ятати:** скоуп `youtube.upload` не дає ні
`channels.list`, ні `videos.delete`. Тестове відео довелось видаляти руками
через Studio — з коду не вийшло б.

---

### Ф6 (архівна довідка) · що вимагалось до перевірки

Передумови (без них не починати):

1. Cloud-проєкт, OAuth **виведений у production** — у Testing авторизація
   протухає **кожні 7 днів** разом із refresh token і вбиває планувальник;
2. **YouTube API compliance audit** — окрема форма, не те саме, що OAuth-верифікація.

Без аудиту `videos.insert` повертає 200, **мовчки** перезаписує
`privacyStatus` на `private`, і публічним це відео вже **не зробити ніколи** —
ні через API, ні руками в Studio.

Робота: resumable upload з локального файлу, `snippet.title` з гачка,
адаптер, `published_private` до аудиту.

### Ф7 · Коментарі з YouTube ✅ (код готовий, чекає на API-ключ)

**Рішення: відповіді з Telegram не робимо.** Тільки вивід коментарів у топік;
відповідати — у самому застосунку. Вивід дає майже всю користь, а вихідний
напрямок довелося б будувати окремо під кожну мережу.

**Ключ, а не OAuth-токен.** `commentThreads.list` вимагає `youtube.readonly`
або `youtube.force-ssl`, а наш грант навмисне несе лише `youtube.upload`.
Коментарі під публічним відео — публічні дані, тож їх читає звичайний
API-ключ: без другого екрана згоди і не чіпаючи токен, від якого залежить
завантаження.

**Полінг по відео, не по каналу.** `allThreadsRelatedToChannelId` потребує
OAuth; `videoId` працює з ключем. Свої відео ми знаємо з `VideoDelivery`,
тож попродуктовий полінг і можливий, і дешевший.

- ✅ `services/youtube_comments.py` — полінг, дедуп, маршрутизація
- ✅ щогодини в планувальнику; `next_daily_slot` виділено окремо, щоб
  добова логіка лишалась тестованою (полінг інакше вигравав би всі порівняння)
- ✅ `order=time` + зупинка на першому вже баченому — не гортаємо всю історію
- ✅ вікно 7 днів: інтерес до Shorts гасне швидко, а полінг усієї історії ріс
  би без меж
- ✅ свої відповіді відсіюються за `authorChannelId`
- ✅ вимкнені коментарі — не помилка, а очікуваний стан
- ✅ 14 тестів

⏳ **Лишилось:** створити API-ключ у Cloud Console (крок для тебе).

---

## Підсумок: де коментарі видно

| Мережа | Видно в Telegram | Чому |
|---|---|---|
| Instagram | ✅ вебхук | |
| Facebook | ✅ вебхук | |
| Threads | ✅ вебхук | topic `moderate`, field `replies` |
| YouTube | ✅ полінг | вебхуків для коментарів немає |
| **TikTok** | ❌ **неможливо** | Content Posting API вміє лише публікувати |

TikTok прикро вдвічі: це наша основна мережа, і саме там формат
«вгадай ціну» розрахований на коментарі. Обійти з нашого боку неможливо —
читання коментарів у їхньому API просто немає.

### Ф9 · Метадані під пошук і рекомендації

Зараз хештеги — статичний список плюс категорія товару. Він однаковий для
всіх мереж і майже однаковий для всіх товарів. Для алгоритмів це слабкий
сигнал: вони не розуміють, що на відео саме овальний джутовий килим
українського виробництва за 400 ₴.

Що зробити:

- **Теги з характеристик товару.** У БД уже лежать виробник, форма, склад,
  висота ворса, розмір — з них виходять точні теги (`овальнийкилим`,
  `джутоваоснова`, `килимукраїна`) замість загального `#килими`.
- **YouTube `snippet.tags`** — окреме поле, не хештеги в описі. Зараз туди
  йде той самий список; має йти конкретика, до 15 тегів.
- **Заголовок YouTube** — зараз лише гачок. Пошук по «килим овальний» його
  не знайде, бо цих слів у назві немає. Треба гачок + назва товару.
- **Перші 100 символів опису** важать найбільше для YouTube-пошуку; зараз
  там назва товару і початок таблиці розмірів.
- **Alt/опис для IG** — Instagram індексує текст у капшені; варто перевірити,
  чи не варто винести ключові слова вище.

Міряти на реальних переглядах, а не вгадувати: спершу зібрати статистику за
2–3 тижні на поточних тегах, потім міняти по одному.

### Ф10 · Посилання на сайт скрізь, де воно клікабельне

| Мережа | Лінк клікабельний | Стан |
|---|---|---|
| Facebook | ✅ | ✅ пряме посилання на товар |
| YouTube | ✅ | ✅ пряме посилання на товар |
| **Threads** | ✅ | ✅ додано |
| TikTok | ❌ | «лінк у профілі» — обмеження платформи |
| Instagram | ❌ | «лінк у профілі» — обмеження платформи |

TikTok та Instagram не роблять посилання в капшені клікабельними — це не
наш вибір, а обмеження платформ. Єдиний робочий шлях там — лінк у профілі.

Лишилось перевірити:

- чи актуальний лінк у біо TikTok та Instagram і чи веде на каталог
- чи варто додавати UTM-мітки, щоб бачити в GA4, звідки прийшли
- Instagram дозволяє кілька посилань у профілі — можна вести на конкретну
  категорію дня, а не на головну

### Ф8 · Health-check токенів

Чотири різні цикли протухання: TikTok 24 год/365 дн · Meta page token ·
Threads 60 дн · Google 1 год + refresh.

Щоденний звіт строків у відео-топік. Інакше про смерть токена дізнаємось
із тиші в стрічці.

---

## 6. Ризики

| # | Ризик | Мітигація |
|---|---|---|
| 1 | Файл видалено, поки Meta ще тягне | чистка по віку, а не одразу |
| 2 | Дубль при ретраї після таймауту | `publishing` до виклику, `external_id` одразу після |
| 3 | Threads: рефреш раніше 24 год → помилка | перевіряти вік токена, не рефрешити «щодня о 03:00» |
| 4 | YouTube у Testing ламається щотижня | не вмикати до production + аудиту |
| 5 | **Нові поля не видно в адмінці** | `SocialSettingsAdmin.fieldsets` не містить `tiktok_auto_enabled`, `tiktok_video_*`, `tiktok_monthly_budget_usd` — Django показує **лише** перелічене. Кожне нове поле вписати у fieldsets, інакше воно невидиме |
| 6 | Ротація: товар вийде двічі | успіх = перша успішна площадка, з коментарем у коді |

**Бюджет не зростає.** Відео одне, генерація одна — $0.12/день лишається.
Природне припущення «п'ять площадок = п'ять цін» хибне. YouTube-квота
безкоштовна.

---

## 7. Перевірити перед реалізацією

Чесно відокремлено від того, що вже підтверджено.

| Питання | Фаза | Чому важливо |
|---|---|---|
| Threads і хештеги: справді один topic tag на пост? | Ф3/Ф4 | **Це моє припущення з практики, не з документації.** Змінює функцію капшену |
| Threads-коментарі: вебхук чи полінг? | Ф5 | визначає обсяг фази |
| FB: звичайне відео чи окремий Reels-ендпоінт? | Ф3 | наявний код кладе звичайне відео; Reels дає більше охоплення |
| Чи YouTube ще вимагає `#Shorts` у назві? | Ф6 | явного скасування в документації не знайдено |
| Threads: суперечність про 90 днів для приватних/публічних профілів | Ф4 | дві сторінки документації кажуть протилежне; не будувати логіку на цьому числі без перевірки |

---

## 8. Заморозка TikTok на час ревʼю — що можна, а що ні

Поки app у статусі `In review`, **конфігурація в кабінеті розробника
заблокована**: форма read-only, замість Save/Submit висить `Recall`.

Але заморожено саме **портал**, не наш код. Різниця критична.

| Об'єкт | Стан | Чому |
|---|---|---|
| Наш Python-код, капшени, монтаж, розклад | ✅ вільно | TikTok його не бачить і не перевіряє |
| Параметри виклику API (privacy, cover, AI-мітка) | ✅ вільно | це рантайм, не конфіг застосунку |
| Scopes, redirect URI, продукти, демо-відео | 🔒 заблоковано | зміна вимагає `Recall` → подача з нуля |
| **Верифікація нового домену** | 🔒 заблоковано | окремий реєстр у порталі |
| Threads / YouTube / Meta | ✅ вільно | інші застосунки, TikTok до них не має стосунку |

**Практичний висновок:** уся робота Ф1–Ф7 портал не чіпає. Відео й далі
лежить на вже верифікованому `mrcarpet24.com`.

⚠️ **Єдина реальна пастка:** якщо колись захочемо роздавати відео з нового
домену або CDN — це вимагає верифікації домену в порталі TikTok, а вона
заблокована. Тому **не міняти хостинг відео до кінця ревʼю.**

`Recall` скидає чергу: подача починається спочатку, а не продовжується.
Тож поки не з'явиться рішення — портал не відкриваємо взагалі.

---

## 9. Ручні кроки — де я зупиняюсь і чекаю

Токени й доступи я беру через браузер сам, але на логіні, паролі, 2FA чи
підтвердженні **зупиняюсь і передаю керування**. Нижче — усі такі точки,
щоб жодна не загубилась.

| # | Крок | Фаза | Хто | Стан |
|---|---|---|---|---|
| 1 | TikTok: вимкнути приватність акаунта після схвалення аудиту | — | **ти** | ⏳ чекає на ревʼю |
| 2 | TikTok: `tiktok_audit_passed = True` після п.1 | — | я | ⏳ після п.1 |
| 3 | Telegram: топік для відео-мереж | Ф2 | я | ✅ `thread_id=1040`, міграція `0018` |
| 4 | Meta: перевірити page token і права на Reels | Ф3 | я | ✅ усе на місці, змін не треба |
| 5 | Threads: увімкнути Threads use case | Ф4 | я | ✅ App ID `1394760422565420` |
| 5a | Threads: callback URLs + 4 дозволи | Ф4 | я | ✅ |
| 5b | Threads: секрет у `ops/` і прод `.env` | Ф4 | я | ✅ бекап `.env.bak-20260721-005531` |
| 6 | Threads: акаунт як Threads tester | Ф4 | я + ти прийняв | ✅ `mr.carpet.shop` |
| 7 | Threads: OAuth-згода | Ф4 | ти натиснув | ✅ токен до **2026-09-19** |
| 8 | Google Cloud: створити проєкт + OAuth client | Ф6 | я, логін — **ти** | ⏳ |
| 9 | Google: вивести OAuth з Testing у production | Ф6 | я, підтвердження — **ти** | ⏳ |
| 10 | YouTube: подати compliance audit | Ф6 | я заповнюю, відправляєш **ти** | ⏳ |
| 11 | YouTube: OAuth-згода на канал | Ф6 | **ти** тисне «Дозволити» | ⏳ |

**Знайдений нік:** `mr.carpet.shop`, не `mr.carpet`. Пошук по `mr.carpet`
знаходив лише чужі схожі акаунти — додати навмання означало б прив'язати до
застосунку чужий профіль. Нік у Threads завжди дорівнює ніку в Instagram.

**Стан прода на 2026-07-21:**

- `THREADS_*` у `.env`, контейнери перезапущені, `oauth_configured: True`
- міграції `0013`–`0018` застосовані, `video_comments_thread_id = 1040`
- токен живий: перевірено реальним викликом `GET /me` → 200, `mr.carpet.shop`
- `can_refresh_now: False` — токену менше доби, запобіжник працює
- **усі нові мережі поки вимкнені**: постить лише TikTok, як і раніше

Секрети — в `ops/` (gitignored), **у git не пушимо**.

---

## 10. Критерій «площадка готова»

Публікації **недостатньо**.

Формат «вгадай ціну» просить людей писати в коментарі. Якщо ми постимо
в мережу, але не читаємо звідти коментарі — ми просимо і мовчимо у відповідь.
Це гірше, ніж не постити туди взагалі.

Площадка вважається готовою, коли працює **і** публікація, **і** доставка
коментарів у відео-топік.
